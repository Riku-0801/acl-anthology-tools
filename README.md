# ACL Anthology Tools

ACL Anthology（aclanthology.org）の使い勝手を向上させるChrome拡張機能。

## 機能一覧

| 機能 | 対象ページ |
|------|-----------|
| 論文ブックマーク管理 | 一覧・イベント・詳細ページ |
| 既読・未読トラッキング | 一覧・イベント・詳細ページ |
| リアルタイム検索・ハイライト | イベントページ（`/events/*`） |
| 既読フィルター | 一覧・イベントページ |
| アブストラクトインラインプレビュー | 論文一覧ページ |
| 著者ポップアップ（最近の論文） | 論文一覧ページ |
| ブックマーク／既読データエクスポート | 拡張機能ポップアップ |

## 開発環境のセットアップ

```bash
npm install
```

## ビルド

```bash
# 開発ビルド（ファイル変更を監視して自動再ビルド）
npm run dev

# 本番ビルド
npm run build
```

ビルド成果物は `dist/` ディレクトリに出力されます。

## Chromeへのインストール

1. `npm run build` を実行して `dist/` を生成する
2. Chrome で `chrome://extensions` を開く
3. 右上の「デベロッパーモード」をオンにする
4. 「パッケージ化されていない拡張機能を読み込む」をクリックし、`dist/` フォルダを選択する

以後、`npm run dev` を起動した状態でソースを変更すると自動再ビルドされます。拡張機能ページの更新ボタンを押すと変更が反映されます。

## テスト

```bash
# 全テストを実行
npm test

# ウォッチモードで実行
npm run test:watch

# カバレッジレポートを生成
npm run test:coverage
```

## 型チェック

```bash
npm run typecheck
```

## 使い方

### ブックマーク

- **追加／解除**: 論文エントリ・詳細ページの「☆ 保存」ボタンをクリックします。もう一度クリックで解除できます。
- **確認・削除**: 拡張機能アイコンをクリックして開くポップアップにブックマーク一覧が表示されます。
- **エクスポート**: ポップアップの「ブックマークをJSONでエクスポート」ボタンからダウンロードできます。

### 既読・未読トラッキング

- **切り替え**: 論文エントリ・詳細ページの「○ 未読」ボタンをクリックすると既読になります。もう一度クリックで未読に戻せます。
- **訪問済み自動記録**: 論文詳細ページを開くと自動的に訪問済みとして記録されます（明示的な既読マークとは区別されます）。
- **フィルタリング**: 一覧・イベントページの「すべて / 未読のみ / 既読のみ」フィルターで表示を切り替えられます。
- **エクスポート**: ポップアップの「既読記録をCSVでエクスポート」ボタンからダウンロードできます（paperId, isRead, markedReadAt, visitedAt 形式）。

### リアルタイム検索（イベントページ）

`aclanthology.org/events/` 配下のページでは、論文一覧の上部に検索バーとフィルターが挿入されます。

- キーワードを入力するとタイトル・著者名・アブストラクトを対象にリアルタイム絞り込みが行われ、マッチした部分がハイライトされます。
- 「abs」ボタンを押してアブストラクトを展開した場合も、本文内の一致箇所がハイライトされます。
- 「すべて / 未読のみ / 既読のみ」フィルターと検索を組み合わせて使えます。
- 「N件 / 全M件」と件数が表示されます。

### アブストラクトプレビュー（一覧ページ）

論文タイトルにカーソルを合わせるとアブストラクトがポップアップ表示されます。ポップアップ内からブックマーク追加・既読マークが操作できます。`Escape` キーで閉じます。

アブストラクトプレビューのオン/オフは拡張機能ポップアップの「設定」タブから切り替えられます。

### 著者ポップアップ（一覧ページ）

論文エントリの著者名にカーソルを合わせると、その著者の最近の論文（最大5件）がポップアップ表示されます。「全著作を見る」リンクから著者ページを新しいタブで開けます。

## プロジェクト構成

```
src/
├── types/index.ts              # 型定義（PaperEntry, BookmarkEntry など）
├── constants/selectors.ts      # DOMセレクター定数
├── utils/
│   ├── domExtractor.ts         # DOM抽出ユーティリティ
│   ├── paperButtons.ts         # ブックマーク・既読ボタン（共通）
│   ├── highlight.ts            # 検索ハイライト
│   └── debounce.ts             # デバウンスユーティリティ
├── services/
│   ├── storage.ts              # chrome.storage.local ラッパー
│   ├── author.ts               # 著者情報フェッチ・キャッシュ管理
│   └── search.ts               # テキストフィルタリング（純粋関数）
├── background/service-worker.ts # HTTPフェッチ委譲（MV3 Service Worker）
├── content-scripts/
│   ├── events.ts               # /events/* 用（検索・フィルター・ボタン注入）
│   ├── listing.ts              # 一覧ページ用（各種ボタン・ポップアップ注入）
│   └── detail.ts               # 論文詳細ページ用（ブックマーク・既読ボタン）
└── popup/
    ├── index.html              # 拡張機能ポップアップ UI
    └── index.ts                # ポップアップロジック
```

## 技術スタック

- TypeScript 5.x（strict mode）
- Chrome Extension Manifest V3
- Vite + vite-plugin-web-extension
- Vitest + happy-dom（テスト）
- データ保存: `chrome.storage.local`（最大10MB）
- 外部サービスへの通信なし（aclanthology.org へのフェッチのみ）
